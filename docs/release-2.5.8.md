# Release 2.5.8

Prepared by Codex on September13,2026 for the explicitly authorized existing preview. Production promotion is separate.

## Result

Job-detail bookmarks sit after the company, with an accessible44px target and existing personal save behavior. Newsletter signup uses20approved invitations with stable IDs/revisions, one shuffled page-memory cycle and stable copy/form while open. The exact footer identifies frequency, unsubscribe and newsletter. Loading and healthy embeds keep Retry hidden; unresolved deadlines and observed errors expose recovery. Founder chips use the requested modest wording. Desktop homepage has a circular closer avatar, exact recipe hover wording and graduation arrows on the photo.

## Verification

-905/905 automated tests under Node22.23.2;19/19 version tests after seal. The first sandbox pass prevented local loopback HTTP tests; the authorized local-server rerun passed all905.
-Independent bounded review found no blocker and passed53focused tests.
-Chrome local checks at390/701/1440 include touch/keyboard arrows, long-company wrapping, save sync/focus, full copy cycles and recovery. Real home Beehiiv form visibly loaded with Retry hidden; no subscription was submitted. Detail recovery uses controlled iframe responses, distinct from provider success.
-All seven configured build stages passed with preview context.348Jobs+43Internships,8themes,3232share HTML/image pairs and3250final HTML pages. These are build counts. Live catalog reads did not write source rows.
-Source fingerprint:62c89507e71217d94035ae0cc4671f7d1e32efe302dd7659b71a67335024fdc7;184public source files. Current privacy receipt and preserved2.5.7predecessor are in docs.

## Limits

Beehiiv OAuth MCP setup is agent access, not website membership matching. No new stored key, analytics event, endpoint, runtime dependency or subscriber lookup was added in this patch. Iframe load is not reliable proof of provider readiness or conversion; error documents can emit load. Physical iPhone Safari remains owner QA. Analytics and automated availability verifier/outbox configuration remain pending the environment-setting decision. Existing receiver deployment and one deduplicated QA email were already proven earlier.

Preview deploy identity, hosted checks and unchanged production readback are recorded separately in the canonical Rasputin2.5.8preview receipt after publication.

## Publication preflight correction

Automatic approval review rejected an upload of auth-feedback.test.cjs because it contained a personal Gmail fixture and credential-shaped mock strings, with repository visibility not yet verified. GitHub metadata confirmed the repository is public. The safer correction replaces personal QA identities in auth-feedback/auth-label with reserved example.invalid addresses and substitutes that identity only inside each offline VM harness. Mock-token comments clarify that values are synthetic; production auth code is unchanged. All28affected tests passed. The public fingerprint and seven-stage build remain unchanged; test files and this receipt are excluded from the shipped surface. The rejected original test payload was not retried.
