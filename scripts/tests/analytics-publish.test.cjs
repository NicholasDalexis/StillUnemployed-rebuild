const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const os=require('node:os');
test('static deploy allowlist excludes server, private docs, fixtures and style guide',async()=>{const {prepare}=await import('../prepare-publish.mjs');const dir=fs.mkdtempSync(path.join(os.tmpdir(),'su-publish-test-'));try{for(const file of ['index.html','analytics.html','style-guide.html','docs/private.md','netlify/functions/private.cjs','scripts/tests/secret.json','js/app.js','css/brand.css','css/style-guide.css','node_modules/secret.js']){fs.mkdirSync(path.dirname(path.join(dir,file)),{recursive:true});fs.writeFileSync(path.join(dir,file),'fixture');}prepare(dir);for(const file of ['index.html','analytics.html','js/app.js','css/brand.css'])assert.ok(fs.existsSync(path.join(dir,'dist',file)));for(const file of ['style-guide.html','docs','netlify','scripts','node_modules','css/style-guide.css'])assert.equal(fs.existsSync(path.join(dir,'dist',file)),false);}finally{fs.rmSync(dir,{recursive:true,force:true});}});
